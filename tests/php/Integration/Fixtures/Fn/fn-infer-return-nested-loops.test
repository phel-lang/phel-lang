--PHEL--
(fn [^int n]
  (loop [outer n]
    (if (php/<= outer 0)
      outer
      (recur (loop [inner outer] (if (php/<= inner 1) inner (recur (php/- inner 1))))))))
--PHP--
(function(int $n): int {
  $outer_1 = $n;
  while (true) {
    if ((($outer_1 <= 0))) {
      return $outer_1;
    } else {
      $outer_1 = (function() use($n,$outer_1) {
        $inner_2 = $outer_1;
        while (true) {
          if ((($inner_2 <= 1))) {
            return $inner_2;
          } else {
            $inner_2 = ($inner_2 - 1);
            continue;

          }
          break;
        }
      })();
      continue;

    }
    break;
  }
});
