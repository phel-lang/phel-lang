(ns phpbench-fixtures\fixture
  (:require phel\test :refer [deftest is]))

(print "Hello, ")
(println "World!")

(defn adder
  {:export true}
  [a b]
  (+ a b))

(deftest my-test
  (is (true? true)))
