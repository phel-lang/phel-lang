interface:
  display_name: "Watch GitHub Issues"
  short_description: "Run autonomous issue polling."
  default_prompt: "Use $watch-gh-issues to process GitHub issues continuously."

policy:
  allow_implicit_invocation: true
