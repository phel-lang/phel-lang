<!-- Generated by `composer error-docs:update`. Do not edit by hand. -->
<!-- Source: src/php/Shared/Exceptions/ErrorCodeCatalog.php -->

# Reader errors (PHEL200-299)

The reader turns forms into the values the analyzer sees, and expands quoting. A code in this range means a quote or an unquote was used where it cannot work.

| Code | Error |
|---|---|
| [PHEL202](#phel202-unquote-splicing-outside-a-collection) | Unquote-splicing outside a collection |
| [PHEL210](#phel210-reader-error) | Reader error |

## PHEL202: Unquote-splicing outside a collection

Enum case: `ErrorCode::INVALID_SPLICE`

The reader found `~@` in a quasiquote with no collection to splice into. Splicing is only defined inside a list, vector, map or set.

```phel
`~@[1 2]
```

**Fix:** Wrap the splice in a collection, or use `~` to unquote a single value.

## PHEL210: Reader error

Enum case: `ErrorCode::READER_ERROR`

The fallback code for a reader error that carries no more specific one: an odd-length map literal, metadata on a value that cannot hold it, an unknown tagged literal. Editor diagnostics report it under this code; the terminal prints the same message with no code in front.

```phel
{:a}
```

**Fix:** Read the message: it names the form the reader rejected.

---

[All error codes](README.md)
