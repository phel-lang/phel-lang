# Repository agent assets

This directory is reserved for phel-lang repo-local agent assets.

Do not place downstream Phel user templates here. Codex treats `.agents/skills` as skills for the current repository, so
installable assets for user projects live in `resources/agents/` instead.
