name: CS auto-fix

on:
  push:
    branches: [chore/bump-php-84]

jobs:
  fix:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: shivammathur/setup-php@v2
        with:
          php-version: 8.4
          coverage: none
          tools: composer

      - name: Install dependencies
        run: composer install --no-interaction --no-ansi --no-progress

      - name: Run CS Fixer
        run: ./vendor/bin/php-cs-fixer fix --allow-risky=yes --using-cache=no

      - name: Run Psalm auto-fix
        run: ./vendor/bin/psalm --config=psalm.xml --alter --issues=MissingParamType || true

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --staged --quiet; then
            echo "No changes"
          else
            git commit -m "style: apply CS Fixer and Psalm auto-fixes"
            git push
          fi
